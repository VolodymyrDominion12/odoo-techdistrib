# =============================================================================
#  __manifest__.py — «паспорт» модуля techdistrib_base
# =============================================================================
#  Odoo вважає директорію модулем ТІЛЬКИ якщо в ній є цей файл.
#  Він читається при кожному старті сервера, тому тут має бути лише
#  статичні дані — жодного коду, жодних імпортів.
# =============================================================================
{
    'name': 'TechDistrib — База дилерів',
    'version': '18.0.1.0.0',
    'category': 'Sales/Sales',
    'summary': 'Дилерська програма: рівні партнерів, коди дилерів, автозниження рівня',

    # 'description' показується на сторінці модуля в Apps.
    # Це reStructuredText, тому НЕ використовуємо тут відступів і спецсимволів.
    'description': """
TechDistrib: база дилерів
=========================

Модуль розширює стандартну картку партнера (res.partner) полями дилерської
програми B2B-дистриб'ютора обладнання.

Що додає
--------
* Ознаку "Дилер" та автоматичний код дилера (DLR-00001).
* Довідник рівнів партнерства: Bronze, Silver, Gold, Platinum.
* Дату початку партнерства та автоматичний розрахунок стажу.
* Метрики продажів дилера: остання продажа, сума підтверджених замовлень.
* Автоматичний статус дилера: новий, активний, сплячий.
* Нічну cron-задачу, що знижує рівень дилера після 6 місяців без покупок.

Закриває вимоги FR-A03 ... FR-A07 з документа бізнес-вимог.
    """,

    # ЗАЛЕЖНОСТІ. Odoo встановить їх ПЕРЕД нашим модулем і гарантує, що
    # їхні моделі вже зареєстровані, коли ми робимо _inherit.
    #   base            — фундамент (res.partner, ir.sequence, ir.cron)
    #   contacts        — адресна книга
    #   sale_management — щоб мати доступ до моделі sale.order
    #                     (нам потрібні замовлення, щоб рахувати продажі дилера)
    #   mail            — щоб мати chatter (message_post) на картці партнера
    'depends': ['base', 'contacts', 'sale_management', 'mail'],

    # ФАЙЛИ ДАНИХ. Читаються СТРОГО зверху вниз.
    # Порядок тут не косметика, а вимога:
    #   1. спершу групи доступу — на них посилаються права в CSV;
    #   2. потім права доступу — вони посилаються на моделі й групи;
    #   3. потім довідкові дані (рівні дилерів) — на них посилаються views;
    #   4. потім views;
    #   5. меню — останніми, бо посилаються на actions з views.
    'data': [
        # 1. Безпека
        'security/techdistrib_security.xml',
        'security/ir.model.access.csv',
        # 2. Дані
        'data/ir_sequence_data.xml',
        'data/dealer_tier_data.xml',
        'data/ir_cron_data.xml',
        # 3. Подання
        'views/dealer_tier_views.xml',
        'views/res_partner_views.xml',
        # 4. Меню
        'views/menus.xml',
    ],

    'installable': True,
    # application=True створює окремий «застосунок» у дашборді Apps
    # і дозволяє мати власне кореневе меню.
    'application': True,
    'license': 'LGPL-3',
}
