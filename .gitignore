# --- Вихідний код Odoo: НЕ наш код, тому не комітимо ---
vendor/odoo/

# --- Python ---
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# --- Odoo runtime ---
data/filestore/
data/sessions/
*.log
odoo.log

# --- Інструменти ---
.idea/
.vscode/
.DS_Store

# --- Середовища та кеші інструментів розробки (не наш код) ---
# .venv-mcp  — ізольоване середовище для Odoo MCP-сервера (див. .agents/skills/odoo-mcp)
# .venv-lint — середовище лінтерів, яке створює scripts/lint.sh
# .pylint-cache — кеш pylint (тримаємо в проєкті, щоб не залежати від ~/.cache)
.venv-mcp/
.venv-lint/
.pylint-cache/
